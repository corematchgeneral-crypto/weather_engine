# Copy to .env and edit if needed
# No API keys required for Open-Meteo

# Fee buffer applied to ask prices
# Fee buffer applied to ask prices
FEE_BUFFER=0.03

# Default minimum edge for signalling
MINIMUM_EDGE=0.12

# Integer settlement mode: true/false
INTEGER_SETTLEMENT_MODE=true
